# AGENTS.md

Codex / AI 维护入口，适用于 `photo-memories` 仓库的日常维护、修复和小步增强。

## 1. 先读的资料
- `README.md`: 项目用途、启动方式、对外说明。
- `ARCHITECTURE.md`: 分层边界、数据流、模块职责、数据库约束。
- `.workbuddy/memory/MEMORY.md`: 长期维护记忆、已知风险、待验证问题、协作规范。

## 2. 项目目标
- 这是一个本地 NAS 回忆相册项目，核心目标是把 NAS 照片在本地扫描、分类、索引并生成回忆展示。
- 优先保证本地可运行、数据不出本机、缓存可重建、数据库可迁移。
- 对用户照片、缩略图、索引数据和日志保持谨慎，默认按“可恢复但不可误删”处理。

## 3. 技术栈
- Python + PyQt6 + SQLite。
- 可能涉及 Everything / `es.exe`、Pillow、`onnxruntime`、DeepFace、OpenCLIP / SigLIP、OpenAI / DeepSeek 兼容 LLM 客户端。

## 4. 分层边界
- `ui/` 负责界面与交互编排，不直接承担复杂存储逻辑。
- `services/` 负责后台任务调度与生命周期。
- `business/` 负责扫描、分类、索引、回忆生成、图像识别等业务流程。
- `infra/` 负责数据库仓储、LLM、图像基础设施和文件系统封装。
- `core/` 负责配置、数据模型、数据库初始化、日志与 checkpoint 等核心能力。

## 5. 隐私与数据规则
- 不访问真实 NAS 路径，除非用户明确要求且已确认风险。
- 不把用户照片、缩略图、数据库内容、API Key、绝对路径写入可提交文本。
- 不对照片做物理删除、移动、重命名，优先标记、过滤或重建索引。
- 所有缓存、缩略图和数据库都应视为可再生但敏感的数据。

## 6. 禁止事项
- 不随意改动数据库 schema、迁移逻辑、表结构或主键约束。
- 不为了修复局部问题而跨层直接调用内部实现，优先走既有接口。
- 不删除已有用户数据目录、缓存目录或模型文件。
- 不修改 `README.md`、`ARCHITECTURE.md`、`.workbuddy/`，除非用户明确要求。
- 不在未请求的情况下运行项目、安装依赖或执行大扫描。

## 7. 变更原则
- 优先做低风险、小范围、可回滚的修改。
- 新增逻辑前先确认当前实现与文档是否一致。
- 能复用现有接口就不要引入新概念。
- 变更应尽量补测试或至少说明测试缺口。

## 8. 任务汇报格式
- `修改文件`: 只列出实际改动的文件。
- `变更摘要`: 用 3-5 条说明做了什么。
- `风险`: 说明是否影响数据、缓存、启动或兼容性。
- `未修改内容`: 明确列出没有动的模块或文件。
- `命令`: 说明是否运行了命令，以及命令类型。

## 9. 默认处理方式
- 遇到不确定的设计选择，先沿用当前约定。
- 遇到破坏性操作，先停下并说明风险。
- 遇到与文档冲突的实现，优先报告差异，再决定是否修正。
