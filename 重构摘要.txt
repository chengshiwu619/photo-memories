================================================================================
NAS 照片回忆 — 重构工作摘要
生成时间: 2026-05-13
当前 Git tag: stage-A0
================================================================================


一、项目概况
────────────────────────────────────────

NAS 照片回忆 — Python 3.10+ / PyQt6 桌面照片管理工具
扫描 NAS 照片，调用 DeepSeek LLM 自动分类+生成回忆文字，瀑布流展示。

核心流程：扫描 → 分类 → 索引（EXIF+缩略图） → 回忆生成 → 瀑布流展示


二、已完成的工作
────────────────────────────────────────

【阶段 0：冒烟测试 ✅】

- 创建 tests/ 目录，5 个测试文件，27 个用例
- 覆盖：config / DB schema / checkpoint / EXIF+缩略图 / CLI
- Git tag: stage-0

【阶段 A0：统一 CheckpointManager + Database ✅】

新建文件：
- checkpoint_manager.py — 泛型断点管理（save/load/clear/pause/stop）
- db_manager.py — 统一数据库连接（contextmanager 自动 commit/rollback、persistent connection、init_tables）

改造 10 个文件，消除 26 处散落 sqlite3.connect 和 3 套重复 checkpoint 代码：
- config.py — 删除 80 行 init_all_tables SQL，改为委托 Database().init_tables()
- scanner/fast_scan.py — 删除 ScanState + checkpoint 代码，改用 CheckpointManager + Database
- scanner/file_scanner.py — 同上
- indexer/photo_indexer.py — 删除 IndexState + checkpoint 代码，改用 CheckpointManager + Database
- classifier/folder_classifier.py — 7 处 sqlite3.connect → Database
- memory/memory_generator.py — 6 处 sqlite3.connect → Database
- ui/app.py — sqlite3.connect → Database.get_persistent_connection()
- ui/components/startup_window.py — 2 处 sqlite3.connect → Database
- ui/components/folder_classifier_dialog.py — 1 处 sqlite3.connect → Database
- clean_data.py — sqlite3.connect → Database
- main.py — import 改为 from db_manager import Database

测试从 27 增至 34 passed
Git tag: stage-A0


三、Git 回退点
────────────────────────────────────────

baseline  → 重构前基线
stage-0   → 冒烟测试完成
stage-A0  → CheckpointManager + Database 完成（当前）

回退命令：git reset --hard <tag>


四、剩余工作
────────────────────────────────────────

【阶段 A1：Pydantic 配置】
- config.py → Pydantic Settings（新增 pydantic-settings 依赖）
- 所有 from config import 改为函数参数注入
- save_config/reload_config 的 global 魔改需要重新设计

【阶段 B：服务层 + Repository】
- 建 Repository 层，搬迁剩余 49 处 SQL 调用
- scanner → scan_service + infra/fs
- classifier → classify_service + infra/llm（加 tenacity 重试）
- indexer → index_service + infra/image（加 draft()）
- memory → memory_service
- 删除 file_scanner.py
- Everything 加 -utf8 参数
- 目标：20 个单元测试

【阶段 C：Pipeline + UI 分离】
- 建 Pipeline Stage 模式（拆分 StartupWorker 的 140 行 run()）
- 拆 MainWindow → controller + widgets
- PipelineWorker 替代 StartupWorker
- BackgroundTaskManager 替代 _bg_threads
- 交互式分类的中断点需特殊处理

【阶段 D：包结构 + 虚拟瀑布流】
- 迁入 src/photo_memories/ 包结构
- pyproject.toml + pip install -e .
- 虚拟瀑布流（最难的单项，Qt 无现成组件）


五、已知问题
────────────────────────────────────────

1. folder_classifier.py 第 107 行有 typo：CLASSASSIFICATION_HISTORY_FILE（多了一个 SS），不影响运行但应修复
2. app.py 中 self.db 是长连接（get_persistent_connection()），关闭时需要手动 conn.close()
3. recommendation.py 的函数签名 db 参数仍是 sqlite3.Connection（由 app.py 的长连接传入），后续阶段 B 建 Repository 时再改


六、关键文件位置
────────────────────────────────────────

rebuild——plan.txt        → 另一个 AI 的完整重构方案（12 个问题 + 4 阶段迁移路径）
checkpoint_manager.py    → 新建的统一断点管理器
db_manager.py            → 新建的统一数据库连接管理器
merge_to_txt.py          → 项目源码合并导出脚本
PROJECT_FULL_DUMP.txt    → 导出的完整源码（~180KB）


七、数据库结构（SQLite，WAL 模式）
────────────────────────────────────────

表名                用途            关键字段
files               文件清单        file_path(UNIQUE), folder_path, file_size, file_mtime, is_image
folder_categories   文件夹分类      folder_path(PK), category(1-4), confidence
photo_metadata      元数据+缩略图   file_id(PK→files.id), date_taken, camera_model, gps_lat/lon, thumbnail_path, is_starred
memories            LLM 回忆        category, title, description, photo_ids(JSON), cover_file_id, is_starred
click_history       用户点击        file_id, folder_path, category, clicked_at
photo_tags          用户标签        file_id, tag(UNIQUE组合)


八、配置项（.env）
────────────────────────────────────────

变量                默认值                          说明
DEEPSEEK_API_KEY    —                               DeepSeek API 密钥（必需）
DEEPSEEK_BASE_URL   https://api.deepseek.com        API 地址
DEEPSEEK_MODEL      deepseek-chat                   模型名称
SOURCE_DRIVE        D:\测试                          照片库根路径
PHOTO_DATA_DIR      {项目根}/storage                 缓存数据目录


九、依赖
────────────────────────────────────────

openai>=1.0.0          # DeepSeek API
Pillow>=10.0.0         # 图片处理
pillow-heif>=1.0.0     # HEIC 支持
python-dotenv>=1.0.0   # .env 配置
PyQt6>=6.5.0           # 桌面 GUI
exifread>=3.0.0        # EXIF 提取
pytest>=9.0.0          # 测试框架（开发依赖）

================================================================================
