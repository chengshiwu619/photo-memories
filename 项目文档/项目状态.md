# 项目状态

## 已完成

- 程序可启动，照片墙、时间线、回忆页可用。
- 缩略图主链路已跑通。
- 启动完整性检查已建立。
- 缩略图维护 CLI 已建立。
- path 标签系统已跑通并可写入 `photo_tags`。
- path 标签噪声清理入口已建立。
- path 标签审计导出入口已建立。
- 后台索引支持保守并发。

## 当前阶段

- 当前重点是缩略图内容识别与视觉标签生产。
- 基础标签优先走 `path`。
- SigLIP 保留为增强来源，不阻塞基础标签闭环。

## 最近验证

- `photo_metadata_total` 在真实库中可用。
- 缩略图大面积正常，失败量很小。
- path 标签可批量写入并可只读审计。
- 历史 path 噪声标签已可安全 dry-run / apply 清理。

## 下一步

- 继续收束 path 标签质量。
- 用小批量方式验证 SigLIP 是否能稳定产出视觉标签。
- 为后续标签治理准备 LLM 审核输入，但暂不联网执行。

## 当前不要做

- 不做特殊回忆自动聚合大改。
- 不做 UI 大改。
- 不改数据库 schema。
- 不全量跑 SigLIP。
- 不做人脸识别批量化。
- 不做 YOLO 批量化。

## 判定标准

- 缩略图链路稳定
- 标签写入可验证
- 审计与清理可回滚
- 不破坏现有浏览体验
