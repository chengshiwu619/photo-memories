# 2026-07-26 工作日志

## UI 改造完成（5个文件修改）

### 1. ui/recommendation.py
- 新增 `reshuffle_photos(photos, shown_ids=None)` 函数
- 已显示照片降权排后，新照片优先，最后 interleave 排序

### 2. ui/components/sidebar.py
- `_NAV_ITEMS` 改为竖排文字+emoji：`"💡\n回\n忆"`, `"📅\n时\n间\n线"`, `"✨\n特\n殊\n回\n忆"`
- 按钮高度 36px → 72px，padding 10px → 6px，新增 line-height: 1.4

### 3. ui/components/virtual_waterfall.py
- `_on_scroll()` 移除 `and not self._all_loaded` 条件（不再阻断无限滚动）
- `append_photos()` 重置 `_all_loaded = False`，超3000张裁剪前半防膨胀

### 4. ui/components/special_memories.py
- 全重写：StackedCard + PokerStack + SpecialMemoriesView
- 折叠态照片30px重叠，展开态86px间距，点击折叠展开，再点击发射 expanded
- ShatterWidget 保持原样
- 类型标签：📅那年今日/👤人物回忆/🎯事件与旅行/🏞️场景回忆/🕐近期回忆

### 5. ui/app.py
- import 新增 reshuffle_photos
- __init__ 新增 `_cat_shown_ids = {}`
- load_memories() 清理 `_cat_shown_ids`
- load_category() 初始化并记录 shown_ids
- `_on_load_more()` 改造：全部加载后触发洗牌续滚

所有修改语法验证通过，未提交git。
