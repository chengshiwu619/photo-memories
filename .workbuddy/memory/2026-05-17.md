# 2026-05-17 工作记录

## 项目文档审查 + 特殊回忆缩略图黑屏诊断
- 全量阅读 ARCHITECTURE.md、MEMORY.md、changelog.md 及核心代码文件（app.py、special_memories.py、recommendation.py、db_manager.py、config.py、photo_indexer.py、timeline_view.py、memories_repo.py、models.py）
- 定位特殊回忆缩略图黑屏根因：load_photos_from_ids() 缺少 thumbnail_path 过滤（NULL/__FAILED__ 未过滤），QPixmap(null) 显示黑色背景
- 整理 v0.3 技术债清单：原有 8 条 + 新发现 5 条（含 P0 bug），共 13 条
- 等待用户确认修复范围后再动手

## 技术债清单审核（#10、#13 撤销）
- 用户确认 #10（_PixmapCache 两份）是刻意隔离设计，三栏互相不影响 → 撤销
- #13（MemoriesRepository 连接策略）经核实 7 个 Repo 全部一致用 self.db.connect() 短连接，不存在不一致 → 撤销
- 最终有效技术债：原有 #1-8（P2 延后 v0.4）+ #9（P0）+ #11（P1）+ #12（P2 待评估）

## 修复 #9: load_photos_from_ids() 缺 thumbnail_path 过滤
- recommendation.py 第 164-172 行：LEFT JOIN → JOIN，加 `pm.thumbnail_path IS NOT NULL AND pm.thumbnail_path != '__FAILED__'`
- 与 load_category_photos_batch / load_starred_photos 过滤条件对齐
- 3 个调用方均无副作用：special_memories.py（黑屏根因）、rank_category_photos（已有 Python 层兜底）、rank_search_photos

## 修复文件夹回忆不显示缩略图
- 根因：discover_folder_memories() 两个 SQL（文件夹聚合 + 单文件夹取 file_id）缺 thumbnail_path 过滤，收集了无缩略图的照片 ID，被 #9 修后 load_photos_from_ids 过滤为空
- 修复：memory_discovery.py 3 处 SQL 加 `AND pm.thumbnail_path IS NOT NULL AND pm.thumbnail_path != '__FAILED__'`
  - discover_folder_memories: 文件夹聚合查询 + 单文件夹取 ID 查询
  - discover_special_date_memories: fallback 查询
- 注：get_photos_by_month_day / get_recent_photos（Repo 层）本身已有过滤，无需修

## 修复时间线/特殊回忆顶栏不回弹
- 根因：_on_page_scroll 只连接了 CategoryPage 的滚动条，TimelineView 和 SpecialMemoriesView 无滚动事件处理
- 修复：
  - app.py: 连接 _timeline_view._scroll 和 _special_view._scroll 的 valueChanged → _on_page_scroll
  - _on_page_scroll: 加 hasattr 保护 memory_summary 和 nav_bar（时间线/特殊回忆无这些组件）
  - _on_nav_changed: 切换 tab 时强制 show top_bar + nav_bar，避免从别的 tab 继承隐藏状态

## 修复特殊回忆栏点击缩略图不显示原图
- 根因：on_photo_clicked() 第 533-535 行，`_current_nav == "special"` 时 `all_photos = []`（空列表），后续 folder_photos 也是空，show_photos([], 0, ...) 无法展示
- 修复：`all_photos = [photo_data]`，直接用点击照片的 dict 构造单元素列表
- app.py 第 535 行：`all_photos = []` → `all_photos = [photo_data]`

## Git 提交 + V0.4 清单
- commit 83dad93: v0.3 所有修复打包提交并推送 GitHub
- 远程仓库迁移：waxzml/photo-memories → chengshiwu619/photo-memories，已更新 origin
- 输出 V0.4 清单到 .workbuddy/V0.4_CHECKLIST.md，共 12 项（8 技术债 + 4 新功能/增强）

## Task 14: IndexStage 完全后台化
- services/background_task_manager.py: IndexStage.run() 改为只统计数量，不调用 index_photos()
- 返回 batch_limit_reached=n>0 触发 Pipeline → BgIndexWorker 接管全部索引
- 效果：启动时不再有任何缩略图生成阻塞，主界面秒出

## Task 15: 特殊回忆滚动修复 + 底部提示
- ui/components/special_memories.py: 移除 _setup_ui() 末尾 addStretch()
- 替换为 _bottom_spacer（初始 0px）+ _end_label（"—— 已展示全部回忆 ——"）
- load_memories() 末尾设 spacer_h = max(200, viewport_h * 0.5)，显示提示

## Task 16: 时间线年份拉球（替代原生滚动条）
- ui/components/timeline_view.py: 完全重写 _YearIndex 类（86-293 行）
  - set_data([(year, count), ...]) 格式输入，_rebuild_dots() 按比例生成年份点+间距点
  - paintEvent: 背景条 + 年份点(实心 r=4 #667eea) + 间距点(空心 r=2 #3a3a5e) + 拉球(实心 r=10 #ff6b6b)
  - 交互: enter→显示拉球, mousePress→开始拖动, mouseMove→跟随+浮层年份指示器, mouseRelease→发射 year_selected
  - _ball_y_to_year() / _year_to_ball_y() 双向映射
- TimelineView 集成:
  - _setup_ui: 隐藏原生滚动条(ScrollBarAlwaysOff)，_year_index 挂 viewport 右侧 28px
  - _build_year_index: 重写为 year_counts → set_data() + set_scroll_range()
  - _on_scroll: 同步 set_scroll_value()
  - resizeEvent: 更新 _year_index 位置/尺寸
- 三个问题全部修复完成
